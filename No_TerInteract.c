void(non-interact)
{
char * = done
do
{
domywish();
if (!path || domywish == 0)
AndyB_exit();
else(!domywish() && =1)
continue;
done = 0
pid = fork();
if (pid = 0)
{
obey = runchild();
}
else
{
wait(done);
done = wexitstatus(done)
}
free(av[0]);
free(path);
}
