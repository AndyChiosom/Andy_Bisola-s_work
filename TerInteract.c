void interact()
{
do
{
prmptAndyBishell();
DoMywish();
if(!domywish)
{
return (null)or exit
}
checkothers();
checkbuiltFunc();
checkpath();
if (!path)
continue;
pid = getpid()
pid = fork()
if (pid == 0)
{
//obey = runchild()
}
else
{
wait(obey)
obey = wexitstatus(obey);
}
free (av[0]);
free(path);
}
}
